name: Deploy Bootstrap Rollout

on:
  push:
    branches:
      - master
    paths:
      - rollouts/bootstrap-5.json

permissions:
  contents: read

jobs:
  rollout:
    uses: ./.github/workflows/rolling-self-update.yml
    with:
      config_file: rollouts/bootstrap-5.json
